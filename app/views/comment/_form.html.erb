<%= form_for([@post, @post.comments.build]) do |form| %> 


    <p>
        <%= form.label :comment %><br> 
        <%= form.text_area :body %> 
    </p>

    <p> 
        <%= form.submit %> 
    <p>
<% end %>