<%= form_for([@post, @post.comments.build]) do |f| %> 
    <div class="field"> 
        <%= f.label :comment %> 
        <%= f.text_field :body %> 
    </div>

    <div class="action">
        <%= f.submit %>  
    </div>
<% end %> 