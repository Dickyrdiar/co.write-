<%= form_for([@post, @post.comments.create]) do |f| %> 
    <div class="form-group"> 
        <%= f.label :comment %><br> 
        <%= f.text_area :body %> 
    </div>

    <div class="action">
        <%= f.submit "create respond", :class => 'btn btn-primary' %>  
    </div>
<% end %> 