<%= form_for([@post, @post.comments.build]) do |f| %> 
    <div class="field"> 
        <%= f.label :comment %><br> 
        <%= f.text_area :body %> 
    </div>

    <div class="action">
        <%= f.submit %>  
    </div>
<% end %> 